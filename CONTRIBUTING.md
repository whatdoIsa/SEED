# 기여 가이드

SEED 저장소의 브랜치 전략, 커밋 메시지, PR 규칙을 정의한다.
모든 변경은 이슈에서 출발해 PR로 develop에 합류한다.

## 브랜치 전략

| 브랜치 | 역할 | 규칙 |
|---|---|---|
| `main` | 배포 | App Store에 출시된 상태만 존재. `release`에서 PR로만 진입. 배포 시 `v<버전>` 태그를 붙이면 TestFlight 워크플로가 실행된다. |
| `release` | 버전 관리 | 다음 버전의 준비 공간. 버전·빌드 번호 확정, QA, 심사 대응 수정. `develop`에서 PR로 받고, 완료되면 `main`으로 PR. 심사 중 수정사항은 `release`에 직접 fix 브랜치로 합류 후 `develop`에 역병합. |
| `develop` | 개발 통합 | 기능 브랜치가 모이는 기본 브랜치. 항상 빌드 가능한 상태를 유지한다. |
| `feature/*` `fix/*` | 작업 | `develop`에서 분기. 하나의 브랜치 = 하나의 이슈. |

흐름: `feature/* -> develop -> release -> main(+tag)`

## 이슈

- 작업 전에 이슈를 먼저 만든다. 템플릿: 버그 리포트, 기능 제안.
- 브랜치 이름에 이슈 번호를 포함한다. 예: `feature/12-friend-duel`, `fix/34-order-sheet-crash`

## 커밋 메시지

형식: `<타입>: <한국어 요약>` (50자 내외, 마침표 없음)

| 타입 | 용도 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변화 없는 구조 개선 |
| `test` | 테스트 추가·수정 |
| `docs` | 문서 |
| `ci` | CI/CD·워크플로 |
| `chore` | 빌드 설정, 리소스 등 그 외 |
| `release` | 버전·빌드 번호 변경 |

- 하나의 커밋은 하나의 논리적 변경만 담는다. 이유가 자명하지 않으면 본문에 왜를 적는다.
- 커밋 전 검증: 앱 변경은 시뮬레이터 빌드 통과, 엔진(JurinKit) 변경은 `swift test` 통과.
- 금지: Co-Authored-By 등 자동 생성 트레일러, 이모지, 영어 요약. 작성자는 항상 개발자 본인 계정 하나만 남긴다.

예:

```
feat: 아레나 리매치 버튼 추가 — 종료 화면에서 같은 시드로 재대결
fix: 주문 시트 매도 스테퍼 상한을 보유 수량으로 제한
release: 1.1.0 빌드 7
```

## PR 규칙

- 하나의 PR은 하나의 기능 또는 수정만 담는다. 리뷰 가능한 크기(변경 500줄 이하 권장)를 유지한다.
- 제목은 커밋 메시지와 같은 형식. 본문은 PR 템플릿 체크리스트를 채운다.
- base 브랜치: 기능·수정은 `develop`, 버전 준비는 `release`, 배포는 `main`.
- CI(엔진 테스트 + 앱 빌드) 통과 후에만 병합한다. 병합 방식은 merge commit(`--no-ff` 동일 효과)을 사용해 기능 단위 이력을 남긴다.
- 1인 개발이므로 self-merge를 허용하되, 병합 전 본인이 diff 전체를 다시 읽는다.

## 코드 스타일

- 이모지를 코드, 주석, 커밋, PR, 로그 문자열 어디에도 쓰지 않는다.
- 과장 수사("완벽한", "혁신적인" 등)와 형식적인 상투어를 쓰지 않는다. 주석은 코드가 스스로 설명하지 못하는 제약과 이유만 적는다.
- 네이밍·주석·문서의 기본 언어: 코드 식별자는 영어, 주석과 문서는 한국어.
- 기존 파일의 컨벤션(주석 밀도, 네이밍, 구조)을 따른다.

## 비밀값

- `SEED/SEED/Learn/TutorSecrets.swift`는 gitignore 대상이다. 절대 커밋하지 않는다.
- CI는 자리표시자를 생성해 빌드하고, TestFlight 워크플로만 저장소 Secret(`TUTOR_CLIENT_TOKEN`)에서 실값을 주입한다.
